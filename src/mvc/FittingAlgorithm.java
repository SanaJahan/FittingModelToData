package mvc;

import java.util.List;

public class FittingAlgorithm {

  double meanOfXCoordinates(List<Double> xCoordinates, int totalX) {
    // iterate Datapoint d : List<Datapoints>
    // d.getxCoordinate +=
    // sum/totalX
    return 0;
  }// needed in k-means hence goes to abstract class

  double meanOfYCoordinates(List<Double> yCoordinates, int totalX) {
    return 0;
  }// needed in k-means hence goes to abstract class



  // for K means
  //method to choose k points from the dataset at random.gives the center points of k clusters
  //method to categorise the point based on distance of it from the center of the cluster
  // hence we need a method to find the center of the cluster-> elbow method to find the right k
  //method to compute ne
  //method to calculate error percentage
  //method to report the cluster assigned to each data point
  // method to improve the result

}
